{
  "name": "pizza-shop-commons",
  "version": "1.0.0",
  "description": "Types and functionalities common to client and server",
  "main": "index.js",
  "types": "index.d.ts",
  "scripts": {
    "build": "tsc && node ./bin/copy-package.js",
    "prebuild": "rimraf dist",
    "postinstall": "npm run build"
  },
  "devDependencies": {
    "chalk": "^4.1.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.0.3"
  },
  "dependencies": {
    "@types/mongoose": "^5.7.36"
  }
}
